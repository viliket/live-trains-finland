type Locomotive {
  vehicleId: Int
}

type Wagon {
  vehicleId: Int
}

extend type Train {
  timeTableGroups: [TimeTableGroup!]
}

type TimeTableGroup {
  arrival: TimeTableRow
  departure: TimeTableRow
}
