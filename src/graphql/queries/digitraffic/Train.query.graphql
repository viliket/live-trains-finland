query Train(
  $trainNumber: Int!
  $departureDate: Date!
  $versionGreaterThan: String!
) {
  train(
    trainNumber: $trainNumber
    departureDate: $departureDate
    where: { version: { greaterThan: $versionGreaterThan } }
  ) {
    ...TrainDetails
  }
}
